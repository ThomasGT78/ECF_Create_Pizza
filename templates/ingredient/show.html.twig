{% extends 'base.html.twig' %}

{% block title %}Ingredient{% endblock %}

{% block body %}
    <h1>Ingredient</h1>

    <table class="table">
        <thead>
        <tr><th colspan="2" class="center big">{{ ingredient.ingredientName }}</th></tr>
        </thead>
        <tbody>
            <tr>
                <th class="center ">Prix</th>
                <td>{{ ingredient.ingredientPrice }} €</td>
            </tr>
            <tr>
                <th class="center">Vegetarian ?</th>
                <td>{{ ingredient.vegetarian ? 'Oui' : 'Non' }}</td>
            </tr>
            <tr>
                <th class="center">Vegan ?</th>
                <td>{{ ingredient.vegan ? 'Oui' : 'Non' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="flex flex-center">
        {#    <a href="{{ path('ingredient_index') }}">back to list</a>#}
        <div><a class="btn btn-warning" href="{{ path('ingredient_edit', {'id': ingredient.id}) }}">edit</a></div>

        {{ include('ingredient/_delete_form.html.twig') }}
    </div>
{% endblock %}
